---
- name: For build server
  hosts: all
  become: yes

  tasks:
  - name: Ensure packages installed
    apt:
      pkg:
      - git
      - default-jdk
      - maven
      state: present
      update_cache: true
